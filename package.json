{
  "name": "ts-functools",
  "description": "functional for typescript",
  "version": "0.0.1",
  "scripts": {
    "compile": "tsc",
    "build": "rm -rf dist && tsc && npm run build_js",
    "list": "tsc --listFiles",
    "build_js": "tsc -p tsconfig_js.json && cp src/*.d.ts dist/"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "prettier": "^1.13.7",
    "typescript": "^2.9.2"
  }
}
